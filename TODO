x -- reduce uses on use
x -- disallow use if use is zero
x -- add stun attack type
x -- load mobs/moves from yaml
x -- handle improper moves by looping until valid input received
x -- add quit command
Make sure input is valid for options given
add intros for each
add debuff/nerf attack (decrease enemy damage)
pretty display
separate output to its own object
add move descriptions array that prints when a move is used
add megamax roll move
fix name alignment when name is longer than 1 tab
add move that damages self and other (sacrifice)


